export const PUBLIC_ROUTES = ['/']

export const AUTH_ROUTES = ['/auth', '/auth/register']

export const PROTECTED_ROUTES = ['/dashboard']

export const DEFAULT_LOGIN_REDIRECT_URL = '/dashboard'

export const API_AUTH_PREFIX = '/api/auth'
